import styled from "styled-components";

export const Button = styled.button`
    padding: 10px;
    border: none;
    border-radius: 3px;
    cursor: pointer;
    :not(:last-child){
        margin-right: 10px;
    }
`